# Mapbox:
. provides many tools to build maps into your website or web-based application:
a. Mapbox GL JS and b. Mapbox.js are both JavaScript libraries you can use to display your Mapbox maps, add interactivity, and customize the map experience in your application.

# Mapbox GL JS : is a client-side JavaScript library for building web maps and web applications. its the newer mapping tool.
 Mapbox GL JS and Mapbox.js :
  . JavaScript libraries use for: 1.display Mapbox maps, 2. add interactivity, 3. customize the map experience in application. 


<!-- 
  <script>

const successCallback = (position) => {
    console.log(position);
    latitude = position.coords.latitude;
    longitude = position.coords.longitude;
  };
  
  const errorCallback = (error) => {
    console.log(error);
  };
  
  const getUserLocation = ()=>{
    navigator.geolocation.getCurrentPosition(successCallback, errorCallback);
}  
    </script>  -->